# Copy this file to .env and fill in your real keys. .env is gitignored and will
# never be committed. Never paste real key values into chat.

OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...
# Only needed if you rent a Lambda Labs GPU instance for Rung 2/3 training (see WRITEUP.md).
LAMBDA_API_KEY=secret_...
